var standardInit = init;

init = function(initData,holtloafData)
{
     standardInit(initData, hotloadData);
	 //This unhidde the country menu in the begin of the game,not needed here, 
	 //i need make a button to unhidde the coutry selection menu
	 //initCountryChoicesDialog();
}